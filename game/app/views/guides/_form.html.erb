<%= form_for([@vgame, @guide]) do |f| %>
  <%if @guide.errors.any?%>
        <div id = "error_explanation">
        <h2>%= pluralize(@tag.errors.count, "error") %> prohibited this guide from being saved:</h2>
        <ul>
          <% @guide.errors.full_messages.each do |message| %>
            <li><%=message%></li>
        <%end%>
        </ul>
      </div>
    <%end%>
      <br>
      <div class="field">
        <%= f.label :title%>
        <%= f.text_field :title, class: 'textbox'%>
      </div>
      <br>
      <div class="field">
        <%= f.file_field :uploaded_file, class: 'myButton'%>
      </div>
      <br>
      <div class="actions">
        <%= f.submit class: 'mybutton' %>
      </div>
    <%end%>
