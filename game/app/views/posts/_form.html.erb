<%= form_for([@topic, @post]) do |f| %>
  <%if @post.errors.any?%>
        <div id = "error_explanation">
        <h2>%= pluralize(@tag.errors.count, "error") %> prohibited this post from being saved:</h2>
        <ul>
          <% @post.errors.full_messages.each do |message| %>
            <li><%=message%></li>
        <%end%>
        </ul>
      </div>
    <%end%>
      <br>
      <div class="field">
        <%= f.label :message%>
        <%= f.text_area :message, size: "150x10",class: 'textarea'%>
      </div>
      <br>
      <div class="actions">
        <%= f.submit class: 'mybutton' %>
      </div>
    <%end%>
